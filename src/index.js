import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { GifExpertApp } from './GifExpertApp';



ReactDOM.render(
    <GifExpertApp />, 
    document.getElementById('root')
);

// Crear un componente llamado GiffExpertApp
// que tendrá un h2 que diga giffexpertapp
//<hr />
